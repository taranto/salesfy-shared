import { should } from "chai";
import { describe } from "mocha";
import { LayerValidate, ValHN } from "../EntryPoint";

export class QuickTest {

	public static test() {
		// describe.only("Something quick to be testable", () => {
		// 	it("should succeed!", (done) => {
		// 		ValHN.getInvalidParams({ idCtChannelView:{a:1}}, [])
		// 		done();
		// 	});
		// });
	}
}
